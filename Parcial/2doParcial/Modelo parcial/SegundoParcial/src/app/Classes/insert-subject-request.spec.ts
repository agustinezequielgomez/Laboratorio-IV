import { InsertSubjectRequest } from './insert-subject-request';

describe('InsertSubjectRequest', () => {
  it('should create an instance', () => {
    expect(new InsertSubjectRequest()).toBeTruthy();
  });
});
