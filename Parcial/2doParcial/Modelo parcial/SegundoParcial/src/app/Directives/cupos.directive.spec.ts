import { CuposDirective } from './cupos.directive';

describe('CuposDirective', () => {
  it('should create an instance', () => {
    const directive = new CuposDirective();
    expect(directive).toBeTruthy();
  });
});
